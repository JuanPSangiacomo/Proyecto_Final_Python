Antes de comenzar a utilizar el programa, instale los requerimientos del mismo mediante el comando: py -m pip install requirements.txt

Una vez hecha la instalacion proceda a abrir una consola en el directorio del proyecto y ejecute el siguiente comando:
streamlit run C:\Users\34671\PycharmProjects\juanp_sangiacomo\deploy_service.py